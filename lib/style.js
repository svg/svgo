'use strict';

// computed flag
// declarations
// parent css rule
// owner node

// flat tree
// Array<[null, root] | [parent, child]>

const getComputedStyle = (ast, node) => {};

const getPropertyValue = (computedStyle, propertyName) => {};

const removeProperty = (computedStyle, propertyName) => {};

const isStyleStatic = (computedStyle, propertyName) => {};
